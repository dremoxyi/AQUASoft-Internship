services:
  database:
    image: postgres
    restart: always
    container_name: postgres_MiniSprint4
    environment:
      POSTGRES_USER: dremoxyi
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: dev
    ports: 
      - 5432:5432